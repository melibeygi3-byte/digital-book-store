# 📘 سناریوی اصلی – فروشگاه کتاب دیجیتال

در این سناریو، کاربر وارد اپلیکیشن می‌شود، کتاب موردنظرش را جست‌وجو می‌کند، آن را انتخاب می‌کند، به سبد خرید اضافه می‌کند، پرداخت را انجام می‌دهد و در نهایت کتاب وارد کتابخانهٔ شخصی او می‌شود.

---

## 🧩 دیاگرام سناریوی اصلی (Mermaid)

```mermaid
flowchart TD

A[ورود کاربر به اپلیکیشن] --> B[نمایش صفحه اصلی و پیشنهادها]
B --> C[جست‌وجوی کتاب یا انتخاب از دسته‌بندی]
C --> D[ورود به صفحه معرفی کتاب]
D --> E{آیا کاربر می‌خواهد کتاب را بخرد؟}

E -->|بله| F[افزودن کتاب به سبد خرید]
E -->|خیر| B

F --> G[مشاهده سبد خرید]
G --> H[پرداخت و تکمیل خرید]
H --> I[افزوده شدن کتاب به کتابخانه کاربر]
I --> J[شروع مطالعه کتاب]
